module FFD
localparam WD = 32; 		//32bits MIPS arch
(
input logic [WD-1:0]d, 
input bit clk, reset, 
input bit enable, 
output logic [WD-1:0]q
);

always@(posedge clk, negedge reset)
	begin

		if(!reset) q <= 0;
				
		else if(enable) q <= d;
		
		
	end

endmodule
